# config/simple_config.py
# Minimal configuration to avoid import errors

# Image parameters
IMAGE_SIZE = 224
NUM_CLASSES = 15

# Simple augmentation config (not directly used but kept for compatibility)
AUGMENTATION_CONFIG = {
    "train": {},
    "val": {}
}